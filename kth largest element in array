class Solution:
    def findKthLargest(self, arr: List[int], k: int) -> int:
        arr=[-num for num in arr]
        heapq.heapify(arr)
        for x in range(k):
            d=heapq.heappop(arr)
        return -d
    
